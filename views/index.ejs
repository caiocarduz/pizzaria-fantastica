<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Pizzaria Fantástica</title>
	<link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="/css/styles.css">
</head>
<body>
	<link rel="stylesheet" href="/css/index.css">
	<form>
		<input type="text" name="busca" placeholder="Qual pizza você procura?" autocomplete="off">
		<button type="submit">Buscar</button>
	</form>
	<main>
			<% pizzas.forEach(element => { %>
				<article>
					<img src= <%= element.img %>  alt="Pepperoni">
					<a href= '/pizzas/<%= element.id %>' >Ver Mais</a>
					<button>+ Add</button>
					<div><%= element.nome %> </div>
					<span><%= element.preco.toLocaleString('pt-br',{style: 'currency', currency: 'BRL'}); %> </span>
					
				</article>
			<% }) %>
	</main>
</body>
</html>